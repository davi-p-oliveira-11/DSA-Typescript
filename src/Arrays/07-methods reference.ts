/*

- **concat**: Joins multiple arrays and returns a copy of the concatenated arrays.  
- **every**: Iterates through all elements of the array, checking a specified condition (function) until `false` is returned.  
- **filter**: Creates an array with all elements that evaluate to `true` according to the specified function.  
- **forEach**: Executes a specific function on each element of the array.  
- **join**: Joins all elements of the array into a string.  
- **indexOf**: Searches the array for specific elements and returns their position.  
- **lastIndexOf**: Returns the position of the last item in the array that matches the search criterion.  
- **map**: Creates another array based on a function that contains the criterion/condition and returns the elements that match the criterion.  
- **reverse**: Reverses the array so that the last item becomes the first and vice versa.  
- **slice**: Returns a new array starting from the specified index.  
- **some**: Iterates through all elements of the array, checking the desired condition (function) until `true` is returned.  
- **sort**: Sorts the array in alphabetical order or according to the specified function.  
- **toString**: Returns the array in string form.  
- **valueOf**: Similar to the `toString` method and returns the array as a string.

*/
