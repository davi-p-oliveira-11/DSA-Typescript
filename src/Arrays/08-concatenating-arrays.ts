const zero: number = 0;
const positiveNumbers: number[] = [1, 2, 3];
const negativeNumbers: number[] = [-3, -2, -1];
let numbers2: number[] = negativeNumbers.concat(zero, positiveNumbers);

console.log(numbers2);
